[project]
name = "simple-nids"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "scapy>=2.5.0",
    "Flask>=3.0.0",
]

[tool.mypy]
strict = true

[tool.ruff]
target-version = "py310"
